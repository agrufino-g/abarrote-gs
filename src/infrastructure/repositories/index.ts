/**
 * Infrastructure Repositories — Barrel Export
 *
 * Concrete implementations of domain repository interfaces.
 * These are the only place where Drizzle ORM touches the domain layer.
 */

export { DrizzleProductRepository } from './DrizzleProductRepository';
